CREATE INDEX bookings_scheduled_for ON public.bookings (scheduled_for);
CREATE INDEX cases_reference ON public.cases (reference);
CREATE INDEX capture_sessions_status ON public.capture_sessions (status);
