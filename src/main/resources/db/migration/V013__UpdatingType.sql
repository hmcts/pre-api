DROP TYPE public.RECORDING_STATUS CASCADE;

CREATE TYPE public.RECORDING_STATUS AS ENUM (
    'STANDBY',
    'INITIALISATION',
    'RECORDING',
    'RECORDING AVAILABLE',
    'PROCESSING',
    'FAILURE',
    'NO RECORDING'
);

ALTER TABLE public.capture_sessions ADD COLUMN status RECORDING_STATUS;