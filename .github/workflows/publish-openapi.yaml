name: Publish OpenAPI specs
on:
# Temporarily changing this from push to pull_request to test it without pushing to master
#  push:
  pull_request:
    branches:
      - "master"

jobs:
  publish-pre-api:
    uses: hmcts/pre-api/.github/workflows/publish-openapi-to-cnp-repo.yaml@main
    secrets:
      SWAGGER_PUBLISHER_API_TOKEN: ${{ secrets.SWAGGER_PUBLISHER_API_TOKEN }}
    with:
      test_to_run: 'uk.gov.hmcts.reform.preapi.openapi.OpenAPIPublisher'
      api_name: 'pre-api'
      java_version: 17
  publish-b2c-api:
    uses: hmcts/pre-api/.github/workflows/publish-openapi-to-cnp-repo.yaml@main
    secrets:
      SWAGGER_PUBLISHER_API_TOKEN: ${{ secrets.SWAGGER_PUBLISHER_API_TOKEN }}
    with:
      test_to_run: 'uk.gov.hmcts.reform.preapi.openapi.OpenAPIPublisher'
      java_version: 17
      api_name: 'pre-api-b2c'
