# Update OpenAPI and Swagger specs

Each CI run on a *pull request to master* should automatically update and commit (if updated) documentation to the PRE API repo, in both swagger (yml) and OpenAPI (JSON) format.

Each CI run on master should automatically upload (if updated) the OpenAPI specs to CNP API Docs repo (cnp-api-docs).
